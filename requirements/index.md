# Requirement Index

<!-- archive-threshold: 5 -->

## Active

| ID | Name | Status | Updated | Description |
|:---|:---|:---|:---|:---|
| REQ-001 | Agent Management Platform | Completed | 2026-04-07 | Local TUI tool to orchestrate multiple Claude Code CLI agents with pub/sub inter-agent communication, group management, session resume, and per-agent system context |
| REQ-002 | Grid Layout + Session-ID Fix | Completed | 2026-04-07 | Agent panes arranged in 2-column grid; fix --session-id + --resume CLI conflict |
| REQ-003 | Configurable Data Directory | Completed | 2026-04-07 | Data stored in project .agent_management/ by default; override via AGENT_MGMT_DATA_DIR env var |
| REQ-004 | Path Input Autocomplete | Completed | 2026-04-07 | Working directory input suggests matching filesystem paths as you type; Tab to accept |
| REQ-005 | CLI Help & Config Display | Completed | 2026-04-07 | --help and --show-config flags print usage and resolved runtime values |
| REQ-006 | Tech Director Role | Completed | 2026-04-07 | Add tech_director AgentRole enum value |
| REQ-007 | Role Templates | Completed | 2026-04-07 | Editable default system prompts per role; t keybinding to manage; auto-fill on role select; user role added |
| REQ-008 | AgentPane Focus Input + Layout | Completed | 2026-04-07 | Input row shows only when pane is focused; AgentPane height adaptive to fill available space |
| REQ-009 | Group Auto-Create Agents | Completed | 2026-04-07 | Creating a group auto-creates PM/Tech Dir/Dev/Tester/User agents named "{group} - {role}", with shared working directory |
| REQ-010 | Delete Group & Agent | Completed | 2026-04-07 | Delete button on AgentPane and GroupPanel; confirmation dialog before deletion; cascade-deletes agents when group is deleted |
| REQ-011 | Native tmux Attach / Detach | Completed | 2026-04-07 | Enter button on AgentPane to attach user's terminal to agent's live tmux pane for native keyboard interaction; Ctrl+B D to return; MCP SSE reconnection on resume |
| REQ-012 | Replace MCP Event Bus with LLM Orchestrator | Completed | 2026-04-08 | v2 pivot: replace MCP event bus with a 6th orchestrator agent that drives a workflow (standard / prototype / research) by dispatching to PM/Tech Director/Dev/Tester/User via tmux send-keys. Completion detection: `<<TASK_DONE>>` marker + 60s silence + 10min stall fallback. Deletes mcp_server.py, pending_events/events tables, topic_list/auto_respond columns. Retains v1 F-03 (Enter pane routing), F-04 (readiness poll), F-06 (error toasts). Requires destructive schema reset. |
| REQ-013 | Terminal Attach Interaction & Output Scroll Fix | Superseded by REQ-015 | 2026-04-08 | Original analysis of native-terminal interaction issues; absorbed wholly into REQ-015 (F-01..F-05) plus the deletion of the OLD input row that REQ-013 explicitly deferred. Never implemented as a standalone REQ. |
| REQ-014 | Quality Hardening for REQ-012 v2 | Completed | 2026-04-08 | Post-hoc code review and test expansion: fixed scrollback offset wraparound in dispatch loop (content-signature dedup), orchestrator tmp-prompt file leak, worker-pane crash vs silence timeout, tester failure retry soft cap; removed dead PaneOutputRefresh and _check_mcp_alive code. Added ~99 tests: 34 orchestrator, 11 workflows, 16 repository, 6 models, 19 session_manager (new file), 13 dispatch_loop integration (new file with FakeSessionManager). Total: 90 → 189 tests. |
| REQ-015 | Native-First Interaction | Completed | 2026-04-08 | Delete OLD per-pane Input/Send row. Read-only preview now renders ANSI colours via Rich Text.from_ansi and implements scroll lock with jump-to-latest button. Add 8-button quick keyboard (Continue / Y / N / Esc / ^C / ↑ / ↓ / ^D). New dedicated InputBox focus catcher forwards every keystroke to the agent's tmux pane via SessionManager.send_raw_keys (pure key forwarding, no batching). Enter on focused preview triggers Attach. New pure-function module `frontend/key_forwarding.py` maps Textual key events to tmux send-keys argv. Absorbs REQ-013 entirely. Adds ~194 tests including a full Textual widget test of the AgentPane key flow. Total: 189 → 383 tests. |
| REQ-016 | Interaction Polish & Dispatch Reliability | Completed | 2026-04-08 | 5-issue polish pass: (F-01) admin controls row is now collapsible via a `⋯` toggle button in the header; Enter button promoted into header. (F-02) punctuation forwarding fixed via new `tmux_args_for_key(event)` helper that prefers `event.character` over `event.key` — Shift-modified punctuation like `!@#$%^&*()` now reaches the agent. (F-03) `start_group`/`stop_group`/`resume_group` parallelised via `asyncio.gather(return_exceptions=True)` — worst-case 6-agent start drops from ~3 minutes to ~30 seconds. (F-04) orchestrator dispatch reliability: parser now accepts self-closing form `<<DISPATCH role="X" text="...">>` (LLMs often drop closing tags); `send_keys` cat fallback removed (broken inside Claude CLI panes); dispatch text newlines normalised to spaces before send (tmux interprets `\n` as Enter); diagnostic warning logged when the pane contains `<<DISPATCH` literal but parse fails. (F-05) **[Partially reverted by REQ-017]** `Step.skill` field added to workflow definitions; all three built-in workflows carry `/req-*` skill hints; orchestrator template gains `技能调用规则` section; PM/TD/Developer/Tester templates updated to mandate skill invocation; `_TEMPLATE_VERSION` bumped to 6. Adds ~58 tests. Total: 383 → 441 tests. |
| REQ-017 | Restore Orchestrator Autonomy | Completed | 2026-04-08 | Reverts the architectural error in REQ-016 F-05 where skill selection was hardcoded in the workflow Step dataclass and Developer template, making the orchestrator a template substitution engine instead of an autonomous decision-maker. Drops `Step.skill` field entirely. Introduces `AVAILABLE_SKILLS` catalogue in `backend/workflows.py` (pure data — 8 `/req-*` skills with descriptions) and `render_skill_catalogue()` helper. New `{{SKILL_CATALOGUE}}` placeholder substituted in the orchestrator's system prompt by `session_manager._render_orchestrator_prompt`. Orchestrator template rewritten to emphasise autonomous decision-making ("你是自主编排者，不是顺序执行器") with workflow reframed as a "typical playbook" not a strict state machine. Worker templates (PM/TD/Dev/Tester) completely simplified — no mention of `Orchestrator`, no hardcoded `/req-*` skill names, no `req-3-code → req-7-verify` chain. Workers only know: "receive task, do task, if task mentions /req-X-Y invoke it, emit `<<TASK_DONE>>`, stop". `_TEMPLATE_VERSION` bumped 6 → 7. Code-level callback contract (worker → supervisor → orchestrator) documented explicitly. Adds ~13 tests covering catalogue presence, autonomous language markers, and regression guards against re-introducing the hardcoding. Total: 441 → 454 tests. |
| REQ-018 | Orchestrator-Centric Refinement | Completed | 2026-04-08 | Three narrow fixes bundled around the orchestrator-as-parent mental model. (F-01) `Supervisor.clear_all` parallelised via `asyncio.gather(return_exceptions=True)` — REQ-016 F-03 missed it; worst-case clear drops from ~seconds-per-session to ~one stop's wall time regardless of group count. (F-02) new `ROLE_CAPABILITIES` dict in `backend/workflows.py` giving each AgentRole a one-line capability description; `render_roster` gains an `include_capabilities=True` default so the orchestrator's rendered system prompt now surfaces each subordinate's "what it can do" alongside its name and pane id — better routing decisions. (F-03) `backend/supervisor.py` module docstring rewritten to explicitly document the orchestrator-as-parent mental model and the "no peer-agent bus" invariant, guarding future maintainers against reintroducing peer-agent thinking. (F-04) full Chinese translation `README.zh-CN.md` at the repo root; both READMEs get a language bar linking each other. (F-05) 16 new tests: clear_all parallelism timing guard + exception isolation + zero-groups no-op; ROLE_CAPABILITIES coverage for every canonical role; `render_roster` default/opt-out modes; integration assertion that the rendered orchestrator prompt contains capability lines. Total: 454 → 470 tests. |

## Archived

| ID | Name | Completed | Description |
|:---|:---|:---|:---|
