# agent-orchestra
Agent Orchestra — a local TUI that conducts a team of Claude Code CLI agents (PM, tech lead, dev, tester, user) through automated workflows, with an LLM orchestrator dispatching roles via tmux.
